let admin,createThumbnail;_d4c‍.x([["default",()=>_d4c‍anonymous]]);_d4c‍.w("firebase-admin",[["default",["admin"],function(v){admin=v}]]);_d4c‍.w("firebase-function-tools/lib/thumbnail",[["default",["createThumbnail"],function(v){createThumbnail=v}]]);


               async function _d4c‍anonymous(object) {
  const thumbnail = await createThumbnail(object)

  if (thumbnail) {
    const { fileDir, downloadURL } = thumbnail

    await admin
      .database()
      .ref(fileDir)
      .update({ thumbnail: downloadURL })
  }

  return
}
