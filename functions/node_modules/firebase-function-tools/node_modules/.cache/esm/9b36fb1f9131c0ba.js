let database,admin;_913‍.x([["default",()=>_913‍.o]]);_913‍.w("firebase-functions",[["database",["database"],function(v){database=v}]]);_913‍.w("firebase-admin",[["default",["admin"],function(v){admin=v}]]);


_913‍.d(database
  .ref('/roles/{roleUid}')
  .onWrite((eventSnap, context) => {
    // Exit when the data is not deleted.
    if (eventSnap.after.exists()) {
      return null
    }

    const roleUid = context.params.roleUid

    return admin
      .database()
      .ref(`role_grants/${roleUid}`)
      .remove()
  }));
